# 11 - Preprocessor

## Status

**Not Started** - planned for Level 12 in `01-ROADMAP.md`.

## Planned lessons

1. `#define` constants and macros
2. `#include`, header guards
3. Conditional compilation `#if`, `#ifdef`, `#ifndef`, `#endif`
4. `#undef`, predefined macros

## Already seen

`#include <stdio.h>`, `<string.h>`, `<ctype.h>` (Day 1/4). `#define` is used
in `C/Project/student_management.c` (`#define MAX_STUDENTS 100`).

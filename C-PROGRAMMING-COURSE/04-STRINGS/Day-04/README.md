# Day-04: Strings

## Learning objectives

- Explain why `'\0'` terminates every string.
- Read strings with `scanf %s`, `%[^\n]`, and `fgets` + `strcspn`.
- Use `strlen`, `strcpy`, `strcmp`, `strcat`, `strchr`.
- Explain `==` vs `strcmp` and string buffer overflow.
- Build small string applications (login, password validation, authentication).

## What I did today

- Studied: strings from the Day-4 class assignment.
- Programs written: `C/Day4/q01-q13` (length, copy, compare, concatenate,
  longer string, password check, `.txt` extension, concat + count, vowels,
  char classification, login system, password creator, secure authentication) -
  all with `.exe`, input files, screenshots.
- Assignment: `C/Assignmenmt/Amit_Kumar_Lab4_Day4_Assignment.docx` (solved:
  13 programs + 10 MCQs + 5 Explain answers).

## MCQs answered (Lab-4)

1. `strlen("Computer")` = B) 8
2. `strcat("Good","Day")` = C) GoodDay
3. `strcmp("Apple","Apple")` = C) 0
4. Output question (char at an index) = B) e
5. Correct string declaration = C) `char str[] = "Hello";`
6. substring question = B) There
7. `strchr`/index question = B) Mango
8. Q8 answer = n
9. Output question = C) John
10. printf("%s", ...) question = A) ProgrammingLanguage / Programming

## Status

- Status: **Completed**
- Confidence: 3/5
- Gap to close: `fgets` + buffer flushing, `strchr`, 2D char arrays, substring
  searches (`strstr`), and buffer-overflow examples - all need practice.

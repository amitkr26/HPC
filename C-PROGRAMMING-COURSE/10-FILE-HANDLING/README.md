# 10 - File Handling

## Status

**Not Started** - planned for Level 11 in `01-ROADMAP.md`.

## Planned lessons

1. `fopen()` and modes (`r`, `w`, `a`, `rb`, `wb`)
2. `fprintf`/`fscanf`, `fgets`/`fputs`
3. `fread`/`fwrite`, binary vs text
4. `fclose()` and error handling (check NULL)
5. `feof`/`ferror`

# 08 - Structures, Unions and Enums

## Status

**Not Started** - planned for Level 9 in `01-ROADMAP.md`.

## What already exists (look-ahead only)

- `08-STRUCTURES-UNIONS/code/student_management.c` - a 397-line project using `struct Student`,
  an array of structs, functions taking `Student *`, `typedef`, and a menu
  loop (add/display/search/update/delete/stats).

This file was written ahead of the course. It is a reference to revisit here -
it is NOT evidence that structures have been learned yet.

## Planned lessons

1. `struct` definition and members
2. Arrays of structs
3. Nested structs, struct pointers `->`
4. `typedef`
5. `union` (memory sharing) and `enum`

## Revisit target

After this module, `08-STRUCTURES-UNIONS/code/student_management.c` should be fully
understood line by line.

# Syntax

Correct C syntax with short comments. One pattern per block, annotated.

```c
/* pattern 1: <what this does> */
syntax here;

/* pattern 2: <when to use this instead> */
other syntax here;
```

> Add common wrong variants with a `// WRONG` comment and the reason.
